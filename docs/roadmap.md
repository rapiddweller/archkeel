# Roadmap

Notes only; no implementation in this milestone.

- M4: `propose`, skill install, `next`. Source: our steering requirement. concept inspired by desloppify @3a7735d, no code
- M5: LLM adapters receive a blind review package: no score, target or expected answer. Verdicts are HYPOTHESIS, bound to the package digest and invalidated by changes to code, contract or rule. concept inspired by desloppify @3a7735d, no code
- Later: finding lifecycle (new / continuing / disappeared / returned) on existing fingerprints. Status never affects the check. concept inspired by desloppify @3a7735d, no code
- Later: resumable review batches, only after more than one review assignment exists. concept inspired by desloppify @3a7735d, no code
- Excluded: a total score as a gate; an empty state as a fallback. concept inspired by desloppify @3a7735d, no code
